apiVersion: apps/v1
kind: Deployment
metadata:
  name: eventhub-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eventhub-mongo
  template:
    metadata:
      labels:
        app: eventhub-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:6.0
          ports:
            - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongo
spec:
  type: ClusterIP
  selector:
    app: eventhub-mongo
  ports:
    - port: 27017
      targetPort: 27017